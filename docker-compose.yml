version: "3.8"
services:
  frontend:
    build:
      context: ./VenueFrontend
    ports:
      - "4200:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./VenueBackend
    ports:
      - "8000:8000"
    env_file:
      - ./VenueBackend/.env
